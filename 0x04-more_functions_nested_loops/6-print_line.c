#include "main.h"

/**
 * print_line - a straight line in the terminal
 * @n: the character should be printed _ severally
 */
void print_line(int n)
{
	if (n <= 0)
	{
		_putchar('\n');
	} else
	{
		int x;

		for (x = 1; x <= x; x++)
		{
			_putchar('_');
		}
		_putchar('\n');
	}
}
